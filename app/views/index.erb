<div class="container">
  <h1>Simplest Possible AJAX</h1>
  <p>This contrived app will simulate a roll of a 6-sided die.</p>

  <form id ="dice_form" method="post" action="/rolls">
    <input id="input" type="text" name="value" placeholder="Enter the roll here"><br>
    <input type="submit" value="Roll the Dice">
  </form>

  <div id="die">
    <% if @roll %>
      <img src="/<%= @roll.value %>.png" title="<%= @roll.value %>" alt="the roll">
    <% end %>
  </div>

</div>
